<?php

namespace App\Services;

class InstagramImage
{
    public static function getImage()
    {
        // $access_token = "3933963451.e029fea.752a5425dbeb4ff6b65afad78c0c58a7";
        // $userid_url = "https://api.instagram.com/v1/users/self/media/recent/?access_token=$access_token";
        // $userid_json = file_get_contents($userid_url);
        // $userid_array = json_decode($userid_json,true);
        
        // $imginsta = array();
        // foreach ($userid_array['data'] as $key) {
        //     foreach ($key['images'] as $keys => $value) {
        //         $imginsta[] = $value['url'];
        //         break;
        //     }
        // }

        return null;
    }
}
